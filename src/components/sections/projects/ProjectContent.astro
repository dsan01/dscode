---
import Prose from '@primitives/Prose.astro'
import { marked } from 'marked'
import  markedFootnote  from "marked-footnote";
import { getLangFromUrl, useTranslations } from '@i18n/utils'
import { type ProjectBasicProps as Props } from '@data/props'
import AsideMenu from '@generics/AsideMenu.astro';

const { project } = Astro.props
const lang = getLangFromUrl(Astro.url)
const t = useTranslations(lang)
const projectContent = project.content ?? ''

const slugify = (text: string) => {
  if (typeof text !== 'string') {
    return ''
  }

  const slug = text
    .toLowerCase()
    .replace(/[^a-z0-9\s-]/g, '')
    .replace(/\s+/g, '-')
    .replace(/-+/g, '-')

  return slug.replace(/^-+|-+$/g, '')
}
const tokens = marked.lexer(projectContent)
const headings = tokens.filter((token) => token.type === 'heading')

const tableOfContents = headings.map((heading) => ({
  text: heading.text,
  depth: heading.depth,
  slug: slugify(heading.text),
}))

const hasFootnotes = tokens.some((token) => token.type === 'footnote');

if (hasFootnotes) {
  tableOfContents.push({
    text: t("prose.footnote.title"),
    depth: 2,
    slug: 'footnotes',
  });
}

const renderer = new marked.Renderer()
renderer.heading = ({ depth, raw, text }) => {
  const slug = slugify(raw)
  return `<h${depth} id="${slug}">${text}</h${depth}>`
}

marked.use(markedFootnote());
marked.use({ renderer})
const htmlContent = (await marked.parse(projectContent)).replace(
  '<section class="footnotes" data-footnotes>',
  '<section class="footnotes" id="footnotes" data-footnotes>'
)
---

<div class='container flex flex-col gap-8 py-8 md:flex-row'>
  <AsideMenu headers={tableOfContents} title={project.title} description={project.description} />
  <div class='basis-2/3'>
    <Prose set:html={marked.parse(htmlContent)} />
  </div>
</div>
